{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --trace-warnings",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@mdi/font": "^7.0.96",
    "nuxt": "3.0.0-rc.13",
    "sass": "^1.56.1",
    "vuetify": "^3.0.1"
  }
}
